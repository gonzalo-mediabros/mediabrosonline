---
interface Props {
  text: string;
  theme?: "light" | "dark";
}

const { text, theme = "light" } = Astro.props;
---

<span
  class:list={[
    "inline-block rounded-full px-5 py-1.5 text-[17px] tracking-wide font-display font-semibold",
    theme === "dark"
      ? "border border-white/80 text-white font-medium"
      : "border border-brand-dark/80 text-brand-dark bg-transparent",
  ]}
>
  {text}
</span>
